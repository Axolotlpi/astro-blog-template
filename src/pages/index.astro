---
    import BaseLayout from '../layouts/BaseLayout.astro';
    import { queries } from '../utils/queries.ts';
    //const stuff = await getContent(query)
---
<BaseLayout title={'example'} description={'this prop overrides siteConfig'} canonicalURL={'this can be fetched and passed in'} >
    
</BaseLayout>